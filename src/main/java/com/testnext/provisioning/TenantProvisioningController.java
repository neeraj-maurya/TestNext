package com.testnext.provisioning;

// TenantProvisioningController intentionally deactivated to avoid duplicate
// request mappings with the canonical `com.testnext.api.controller.TenantController`.
// If you need to restore tenant provisioning endpoints, migrate logic into the
// API controller or rename endpoints to avoid collision.
public class TenantProvisioningController {
    // Placeholder - not a Spring bean.
}
